var a: int
var b: int

var N: int

fun main(): int =
    a = 5,
    b = 3,
    if a < 3 then
        b = new(a,b)
    end,
    a = new(a,b),
    return a
    
fun new(a:int, b:int): int = 
    return a + b 

   